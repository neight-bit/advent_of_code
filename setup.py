from setuptools import setup, find_packages

setup(
    name='aoc',
    version='2023',
    author='neight.bit',
    author_email='neight.bit@protonmail.com',
    packages=find_packages(),
    install_requires=[],
)